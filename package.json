{
	"devDependencies": {
		"markdownlint": "^0.31.1",
		"markdownlint-cli": "^0.37.0"
	},
	"scripts": {
		"lint:md": "command -v markdownlint >/dev/null && markdownlint --config .markdownlint.yaml \"${@:2}\" || npx markdownlint --config .markdownlint.yaml \"${@:2}\"",
	}
}
