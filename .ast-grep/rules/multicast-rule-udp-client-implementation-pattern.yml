# .ast-grep/multicast-rule-udp-client-implementation-pattern.yml
id: udp-client-implementation-pattern
rule:
  pattern: |
    class MulticastUDPClient($$$):
      $$$
      def __init__(self, $$$):
        $$$
      def $METHOD(self, $$$):
        $$$
language: python
message: "Verify UDP client implementation follows project standards"
files:
  - tests{/**/,/}test_*.py
severity: hint
description: |
  Mcast server must implement doStep method with proper error handling
  and follow project multicast server guidelines.
