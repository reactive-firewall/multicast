# .ast-grep/multicast-rule-server-implementation-pattern.yml
id: multicast-rule-server-implementation-pattern
rule:
  pattern: |
    class Mcast$TYPE:
      $$$
      def doStep(self, $ARGS):
      $$$
language: python
message: "Verify Mcast server implementation follows standard pattern"
files:
  - multicast{/**/,/}*.py
severity: error
