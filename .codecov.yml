coverage:
  status:
    project:
      default: on
      target: 95%
      threshold: 0%
      base: auto 
      unittests:
        target: 80%
        flags: unittests
        paths:
          - "tests/*"

flags:
  unittests:
    paths:
      - "tests/*"
    joined: false
