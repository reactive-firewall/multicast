coverage:
  status:
    project:
      default: on
        target: 95%
        base: auto
      tests:
        target: 80%
        flags: tests
        paths:
          - tests

flags:
  unittests:
    paths:
      - tests
    joined: false
