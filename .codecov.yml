coverage:
  status:
    project:
      default:
        target: 95%
        threshold: 0%
        base: auto
      multicast:
        target: auto
        flags:
          - multicast
        paths:
          - "multicast/*.py"
          - "setup.py"
      unittests:
        target: 80%
        flags:
          - unittests
        paths:
          - "tests/*"

flags:
  unittests:
    paths:
      - "tests/*"
    carryforward: true
    joined: false
  multicast:
    paths:
      - "multicast/*.py"
      - "setup.py"
    joined: false
    carryforward: true
