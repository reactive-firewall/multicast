coverage:
  status:
    project:
      default: on
      base: auto
      target: 95%
      unittests:
        target: 80%
        flags: unittests
        paths:
          - tests

flags:
  unittests:
    paths:
      - tests
    joined: false
