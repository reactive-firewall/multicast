coverage:
  status:
    project:
      default: on
        base: auto
      multicast:
        target: 95%
        paths:
          - multicast
      unittests:
        target: 80%
        flags: unittests
        paths:
          - tests

flags:
  unittests:
    paths:
      - tests
    joined: false
